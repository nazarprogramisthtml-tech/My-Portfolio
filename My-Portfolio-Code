<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
    <title>Назар Бучок - Portfolio</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/devicons/devicon@latest/devicon.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        /* ГЛОБАЛЬНЫЕ НАСТРОЙКИ */
        * { box-sizing: border-box; }
        
        body, html {
            margin: 0; padding: 0;
            width: 100%; min-height: 100vh;
            /* НОВЫЙ ФОН: Мягкий градиент вместо чисто белого */
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            font-family: 'Segoe UI', Roboto, sans-serif;
            color: #1a1a2e;
            display: flex;
            flex-direction: column;
            overflow-x: hidden;
        }

        /* ИСПРАВЛЕННЫЕ АНИМИРОВАННЫЕ ТОЧКИ */
        .bg-dots {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            /* Точки сделаны темнее (#909090) для лучшей видимости */
            background-image: radial-gradient(#909090 1.5px, transparent 1.5px);
            background-size: 30px 30px;
            z-index: -1;
            opacity: 0.6; /* Легкая прозрачность, чтобы не рябило */
            animation: moveBg 30s linear infinite;
        }

        @keyframes moveBg {
            from { background-position: 0 0; }
            to { background-position: 30px 30px; }
        }

        /* ОСНОВНОЙ КОНТЕЙНЕР (ЦЕНТРИРОВАНИЕ) */
        .wrapper {
            width: 100%;
            max-width: 500px;
            margin: 0 auto;
            padding: 20px;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        header {
            width: 100%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding: 10px 0;
        }

        .logo {
            font-weight: bold; font-family: monospace; font-size: 1.1rem;
            text-shadow: 1px 1px 2px rgba(255,255,255,0.5);
        }

        .lang-switch {
            display: flex;
            background: rgba(240, 240, 240, 0.8);
            padding: 4px;
            border-radius: 12px;
            gap: 4px;
            backdrop-filter: blur(5px);
        }

        .lang-btn {
            border: none;
            background: transparent;
            padding: 6px 14px;
            border-radius: 10px;
            cursor: pointer;
            font-weight: 600;
            font-size: 0.8rem;
            color: #666;
            transition: 0.3s;
        }

        .lang-btn.active {
            background: #1a1a2e;
            color: #ffffff;
        }

        /* КАРТОЧКИ С ЭФФЕКТОМ СТЕКЛА */
        .card {
            width: 100%;
            background: rgba(255, 255, 255, 0.65); /* Больше прозрачности для эффекта стекла */
            backdrop-filter: blur(20px); /* Сильное размытие фона под карточкой */
            -webkit-backdrop-filter: blur(20px);
            border-radius: 25px;
            padding: 25px;
            margin-bottom: 15px;
            border: 1px solid rgba(255, 255, 255, 0.4);
            box-shadow: 0 8px 32px rgba(31, 38, 135, 0.1);
            text-align: center;
        }

        .tiktok-header { font-size: 2.8rem; margin-bottom: 5px; color: #000; }
        .tiktok-info { font-weight: 800; font-size: 1.2rem; color: #ff0050; text-shadow: 1px 1px 2px rgba(255,0,80,0.2);}
        .birth-date { font-size: 0.9rem; color: #444; margin-top: 6px; font-weight: 500; }

        .hero h1 { font-size: 2.4rem; margin: 0; line-height: 1.1; color: #1a1a2e; }

        /* МИГАЮЩИЙ КУРСОР */
        .subtitle {
            font-family: 'Courier New', monospace;
            color: #007aff;
            font-weight: bold;
            margin-top: 8px;
            font-size: 1.1rem;
            text-shadow: 0 0 10px rgba(0,122,255,0.3);
        }

        .cursor {
            display: inline-block;
            width: 10px;
            height: 1.1rem;
            background-color: #007aff;
            margin-left: 4px;
            vertical-align: middle;
            animation: blink 1s step-end infinite;
            box-shadow: 0 0 8px rgba(0,122,255,0.5);
        }

        @keyframes blink {
            from, to { opacity: 0; }
            50% { opacity: 1; }
        }

        .tg-link {
            text-decoration: none;
            width: 100%;
            background: linear-gradient(135deg, #1a1a2e, #2a2a4e);
            color: #ffffff;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 18px;
            border-radius: 22px;
            margin-bottom: 15px;
            transition: transform 0.2s, box-shadow 0.2s;
            box-shadow: 0 5px 15px rgba(26, 26, 46, 0.3);
        }

        .tg-link:active { transform: scale(0.98); box-shadow: 0 2px 10px rgba(26, 26, 46, 0.2); }

        .skills-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
        }

        .skill-item { display: flex; flex-direction: column; align-items: center; gap: 5px; }
        .skill-item i { font-size: 2rem; color: #333; transition: 0.3s; }
        .skill-item:hover i { transform: scale(1.1); color: #007aff; }
        .skill-item span { font-size: 0.8rem; font-weight: 600; color: #555; }

        /* АДАПТАЦИЯ ДЛЯ МАЛЕНЬКИХ ЭКРАНОВ */
        @media (max-height: 650px) {
            .wrapper { padding: 15px; }
            .card { padding: 15px; margin-bottom: 10px; }
            header { margin-bottom: 15px; }
            .hero h1 { font-size: 2rem; }
            .tiktok-header { font-size: 2.2rem; }
        }
    </style>
</head>
<body onclick="requestFull()">

    <div class="bg-dots"></div>

    <div class="wrapper">
        <header>
            <div class="logo">Portfolio.init()</div>
            <div class="lang-switch">
                <button class="lang-btn" onclick="setLang('ua')">UA</button>
                <button class="lang-btn active" id="btn-ru" onclick="setLang('ru')">RU</button>
            </div>
        </header>

        <section class="card">
            <div class="tiktok-header"><i class="fa-brands fa-tiktok"></i></div>
            <div class="tiktok-info">@nazar4es.com</div>
            <div class="birth-date" id="birth-text">Дата рождения: 28.10.2014</div>
        </section>

        <section class="card hero">
            <h1>Бучок Назар</h1>
            <div class="subtitle">
                <span id="role">Python-разработчик</span><span class="cursor"></span>
            </div>
        </section>

        <section class="card">
            <h2 id="about-title" style="margin-top:0; font-size: 1.2rem; margin-bottom: 10px;">Обо мне</h2>
            <p id="about-text" style="line-height: 1.5; color: #444; margin: 0; font-size: 0.95rem; font-weight: 500;">
                Я — Разработчик с опытом в создании современных приложений. Специализируюсь на автоматизации, Python-скриптах и веб-технологиях.
            </p>
        </section>

        <a href="https://t.me/B0YTS0VSK1Y_CLUB" target="_blank" class="tg-link">
            <span id="contact-btn" style="font-weight: bold; font-size: 1.05rem;">Связаться со мной</span>
            <span style="font-size: 0.8rem; opacity: 0.8; margin-top: 2px;">@B0YTS0VSK1Y_CLUB</span>
        </a>

        <section class="card">
            <div class="skills-grid">
                <div class="skill-item"><i class="devicon-python-plain"></i><span>Python</span></div>
                <div class="skill-item"><i class="devicon-javascript-plain"></i><span>JS</span></div>
                <div class="skill-item"><i class="devicon-cplusplus-plain"></i><span>C++</span></div>
                <div class="skill-item"><i class="devicon-html5-plain"></i><span>HTML5</span></div>
            </div>
        </section>
    </div>

    <script>
        // Полноэкранный режим по клику
        function requestFull() {
            let el = document.documentElement;
            if (el.requestFullscreen) el.requestFullscreen();
            else if (el.webkitRequestFullscreen) el.webkitRequestFullscreen();
            document.body.onclick = null; 
        }

        // Данные для перевода
        const langData = {
            ru: {
                role: "Python-разработчик",
                aboutTitle: "Обо мне",
                aboutText: "Я — Разработчик с опытом в создании современных приложений. Специализируюсь на автоматизации, Python-скриптах и веб-технологиях.",
                contact: "Связаться со мной",
                birth: "Дата рождения: 28.10.2014"
            },
            ua: {
                role: "Python-розробник",
                aboutTitle: "Про мене",
                aboutText: "Я — Розробник з досвідом у створенні сучасних застосунків. Спеціалізуюсь на автоматизації, Python-скриптах та веб-технологіях.",
                contact: "Зв'язатися зі мною",
                birth: "Дата народження: 28.10.2014"
            }
        };

        // Функция смены языка
        function setLang(lang) {
            document.getElementById('role').innerText = langData[lang].role;
            document.getElementById('about-title').innerText = langData[lang].aboutTitle;
            document.getElementById('about-text').innerText = langData[lang].aboutText;
            document.getElementById('contact-btn').innerText = langData[lang].contact;
            document.getElementById('birth-text').innerText = langData[lang].birth;

            document.querySelectorAll('.lang-btn').forEach(btn => btn.classList.remove('active'));
            event.target.classList.add('active');
        }
    </script>
</body>
</html>
